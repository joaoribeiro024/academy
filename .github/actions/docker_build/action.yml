name: 'Hello World'
description: {{ action-description }}
inputs:
  username:
    description: 'Says hello to the user'
    required: false
    default: 'World'
runs:
  using: composite
  steps:
    - run: |
        echo "Hello World"
      shell: bash